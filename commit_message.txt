feat: show string system sensors in dashboard

- publish system monitor values every 5 seconds instead of every 30 seconds
- send initial system sensor samples after startup to reduce default 0 display
- add om_system_wifi_ssid string sensor for the connected WLAN name
- retain MQTT sensor data/bson messages so reconnecting clients get latest values
- allow STRING sensor metadata in the web dashboard and render text values as sensor tiles
- update Flutter service worker hash for the patched web bundle
